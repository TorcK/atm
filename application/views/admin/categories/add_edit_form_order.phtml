<select name="order" class="form-control" id="formOrder">
    <?
    $count_for = $count + 1;
    for ($i = 1; $i <= $count_for; $i++) {
          $selected = '';
          if ($one_order) {
               $selected = ($one_order == $i) ? 'selected = "selected"' : '';
          } else {
               $selected = ($count_for == $i) ? 'selected = "selected"' : '';
        }?>
        <option value="<?=$i;?>" <?=$selected?> > <?=$i;?></option>
    <? }?>
</select>